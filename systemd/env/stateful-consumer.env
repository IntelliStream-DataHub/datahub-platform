# /etc/datahub/stateful-consumer.env
#
# JVM flags for datahub@stateful-consumer: a fixed heap, pre-touched on the NUMA node the
# placement drop-in binds it to. One ordered Neo4j writer; memory-light.
# No heap dump on OOM (the JVM default); see "Diagnostics" in the README.
# systemd joins the backslash-continued lines.
JAVA_OPTS=-Xms16g -Xmx16g -XX:+AlwaysPreTouch -XX:+UseTransparentHugePages -XX:+UseCompactObjectHeaders \
 -XX:+ExitOnOutOfMemoryError \
 -Xlog:gc*:file=/var/log/datahub/stateful-consumer/gc.log:time,uptime:filecount=10,filesize=64m
