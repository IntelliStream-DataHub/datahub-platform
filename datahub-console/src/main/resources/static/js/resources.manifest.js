// Manifest for the resources-page JS bundle (see ASSETS.md).
// Build-time only - lists the source files, in order, via Sprockets-style require directives.
// Page behaviour for the resources page (root-list context menu: graph export / import). Depends
// on the app bundle (DhContextMenu, Flash, $L), which every page loads first via layout/main.html.
//
//= require resources-page.js
