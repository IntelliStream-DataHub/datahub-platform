# Centralized dependency & plugin versions for ALL modules.
# Root gradle.properties values propagate to every subproject, so each version lives
# here once. Plugin versions are consumed by settings.gradle (pluginManagement); library
# versions are referenced in module build.gradle files as ${pulsarVersion} etc.

# --- Gradle plugins ---
springBootVersion=4.1.0
dependencyManagementVersion=1.1.7

# --- Published artifact versions ---
# The two modules published to a Maven repository. Both read these with a
# 0.1.0-SNAPSHOT fallback, so leaving them undefined meant every artifact ever
# published carried the snapshot version regardless of what was released.
# Override on the command line for a one-off build: -PjavaSdkVersion=0.2.0
apiModelVersion=0.1.0-SNAPSHOT
javaSdkVersion=0.1.0-SNAPSHOT

# --- Shared library versions ---
pulsarVersion=4.0.11
jacksonVersion=3.1.4
feignVersion=13.6
anthropicSdkVersion=2.52.0
